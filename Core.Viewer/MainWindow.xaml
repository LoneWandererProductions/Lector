<Window x:Class="Core.Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:Common.Converter;assembly=Common.Converter"
        xmlns:local="clr-namespace:Core.Viewer"
        mc:Ignorable="d"
        Title="Analyzer Viewer" Height="450" Width="900">

    <Window.DataContext>
        <local:AnalyzerViewModel />
    </Window.DataContext>
    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVisibility" Collapse="True" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <ProgressBar Value="{Binding ProgressValue}" Minimum="0" Maximum="100" Grid.Row="2" />

        <!-- Analyzer selection and controls -->
        <StackPanel Orientation="Horizontal" Margin="5,5,5,5" Grid.Row="0" Grid.ColumnSpan="2">
            <ComboBox ItemsSource="{Binding Analyzers}"
                      SelectedItem="{Binding SelectedAnalyzer}"
                      Width="200" DisplayMemberPath="Name" />
            <Button Content="Run Analyzer" Margin="5,0" Command="{Binding RunAnalyzerCommand}" />
            <Button Content="Select Folder" Margin="5,0" Command="{Binding SelectFolderCommand}" />
            <TextBox Width="200" Margin="5,0"
                     Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!-- Diagnostics display -->
        <ListView Grid.Row="1" ItemsSource="{Binding DiagnosticsView}" Grid.ColumnSpan="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Status" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Diagnostic.SeveritySymbol}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontFamily="Segoe UI Emoji"
                                           FontSize="14" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Analyzer" DisplayMemberBinding="{Binding Diagnostic.Name}" />
                    <GridViewColumn Header="File" DisplayMemberBinding="{Binding Diagnostic.FilePath}" />
                    <GridViewColumn Header="Message" DisplayMemberBinding="{Binding Diagnostic.Message}" />

                    <GridViewColumn Header="Actions" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Open File"
                                            Command="{Binding OpenFileCommand}"
                                            Margin="2" />
                                    <Button Content="Fix"
                                            Command="{Binding FixCommand}"
                                            Visibility="{Binding CanFix, Converter={StaticResource BoolToVisibility}}"
                                            Margin="2" />
                                    <Button Content="Ignore"
                                            Command="{Binding IgnoreCommand}"
                                            Margin="2" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>