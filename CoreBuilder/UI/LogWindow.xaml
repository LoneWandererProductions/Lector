<Window x:Class="CoreBuilder.UI.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log Window" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Actions">
                <MenuItem Header="Clear Log" Click="ClearLog_Click" />
                <Separator />
                <MenuItem Header="Go to Top" Click="GoTop_Click" />
                <MenuItem Header="Go to Bottom" Click="GoBottom_Click" />
            </MenuItem>

            <MenuItem Header="_Options">
                <MenuItem x:Name="AutoScrollCheck" Header="Auto Scroll" IsCheckable="True" IsChecked="True" />
                <MenuItem x:Name="ShowTimestampCheck" Header="Show Timestamp" IsCheckable="True" IsChecked="False" />
            </MenuItem>
        </Menu>

        <ListBox x:Name="LogList" 
                 Margin="10"
                 FontFamily="Consolas"
                 BorderThickness="0"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding Mode=OneWay}" 
                             TextWrapping="Wrap" 
                             IsReadOnly="True" 
                             BorderThickness="0" 
                             Background="Transparent"
                             Padding="0" 
                             Margin="0"
                             Style="{x:Null}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>